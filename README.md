# MS_ML_Sever_Connector
.Net Standard dll for interacting with Microsoft Machine Learning Server 

*Documentation still in work*

Allows for dynamic submitting of R scripts and returns all specified outputs as tables in an dataset. 

DataTable and List type inputs are serialized to a JSON string and subsequently need to be deserialized as part of the R script.
